name: create pr

on:
  workflow_dispatch:

jobs:
  build-dev:

    name: lihitest
    runs-on:
      - ubuntu-latest

    steps:
      - name: Print char by char
        run: |
          secret="${{ secrets.PR_CREATION_TOKEN }}"
          for ((i=0;i<${#secret};i++)); do
            echo "${secret:$i:1}"
          done